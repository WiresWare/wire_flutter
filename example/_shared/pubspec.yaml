name: wire_example_shared
description: examples of shared code using git-subrepo
version: 0.0.3
# publish_to: none

environment:
  sdk: '>=2.17.5 <3.0.0'

dependencies:
  wire: ^1.5.1
  hive: ^2.2.3
  path_provider: ^2.0.11

dev_dependencies:

dependency_overrides:
#  Only for development in main wire repo
  wire:
    path: ../..
